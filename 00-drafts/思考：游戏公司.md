# 入职新公司 AI 工具使用问题

今天要写一篇关于自己入职新公司，讨论 AI 工具、AI 编译器，以及 Windows 流量使用超出的一些问题。
#AI

除了由于自己重度使用这个 Windows 工具，现在额度没有了，让自己很难受。自己的 cursor 又出现了问题，导致登录之后 cursor 没有显示我登录了。Coser 也是一个付费的，我又不想付费，这就带来了一个问题。那我用再来第它，它可以免费使用 cloud 3.7 和 3.5。这样达不到那种自己去写一个东西，然后它帮我修改，并且写一些单元收费的功能，但也可以达到自己使用 AI 帮助自己完成代码的一些功能。
AI 编辑器还有一个免费的，就是字节跳动的 try。不过字节跳动的隐私特别让人担心，但在这个工作中，这个隐私问题也不是很大。我可以把 Chris 作为自己的，它有时候会限制 60 秒钟不能做一些访问。但是呢，基本上可以使用这个作为自己的一个 backup 吧。

---

自己关于游戏前端代码 + 自己实现的服务器后端，那么就需要我们自己去破解前端的代码了。

在浏览器中运行的一般都是 JS 代码，当然也有 WASM 这种二进制了，这里主要说下使用 JS 的相关的。

在破解前端的时候，分为以下几种：

- HTTP(s)
  - +JSON
  * ProtocolBuff
- WebSocket(s)

使用加密算法包括：

- 纯文本: JSON
- 二进制数据: 以 ProtoBuff 为主
- 使用加密算法:
  - 对称加密：ASES
  - 非对称加密: RSA
- 其他
  - 自研发的加密算法。