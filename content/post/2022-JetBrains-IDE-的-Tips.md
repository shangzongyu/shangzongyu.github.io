---
title: JetBrains IDE 的 Tips
description: 介绍使用 JetBrains IDE 过程的的一些 Tips。
slug: jetbrains-ide-tips
date: 2022-10-27 15:13:25+0000
image: https://raw.githubusercontent.com/shangzongyu/blog-image/main/2025/piclist_20251114_2022-JetBrains-IDE-的-Tips.webp
tags:
  - tips
weight: 1
---
> 介绍使用 JetBrains IDE 过程的的一些 Tips。

## Goland Tips

### 行排序

通过 `⌘ ⇧ A` (macOS) / `Ctrl+Shift+A` (Windows/Linux) 打开 Actions，搜索 `Sort Lines` 进行排序。

### Open Blank Diff Window

通过 `⌘ ⇧ A` (macOS) / `Ctrl+Shift+A` (Windows/Linux) 打开 Actions，搜索 `Blank Diff Window` 会打开左右里两个用户 differ 的窗口。

### Scratch files

> Scratch file 不仅仅是纯文本，它语法高亮、代码不全以及和文件类型相关的特性。
> Scratch file 随着 IDE 存在，没有和某个项目绑定。

操作：`Select File | New | Scratch File`，或者如下快捷键

or press on macOS or Windows/Linux。

- macOS：`⇧ ⌘ N`
- Windows/Linux：`Ctrl+Alt+Shift+Insert`

### 多光标

启动方式 1：

1. 启动多光标：按两次 `⌥` (macOS)/ `Ctrl` (Windows/Linux)，并且不释放它
2. 接着按向上箭头或者向下箭头进行选择

启动方式 2：

按住 `⌥⇧` macOS）/ `Shift+Alt` (Windows/Linux) 然后使用光标进行选择

启动方式 3：

按着 `⌥` (macOS)/ `Alt` (Windows/Linux) 使用鼠标进行选择多光标的操作。

> 删除所有的按 `Escape` 键值。

### 折叠所有 / 展开所有

- macOS：`⇧⌘-` / `⇧⌘+`
- Windows/Linux：Ctrl + Shift + NumPad + /  Ctrl + Shift + NumPad –

> 上面的快捷键关于折叠和展开文件，如果有选择一部分，那么会折叠和展开的部分。

折叠当前段：

- macOS：`⌥⌘-` / `⌥⌘`
- Windows/Linux：`Ctrl+Alt+NumPad` + / `Ctrl+Alt+NumPad –`

### 最近打开的文件和位置

- 最近打开的文件
  - macOS：`⌘E`
  - Windows/Linux：`Ctrl+E`
- 最近打开的位置
  - macOS：`⌘⇧E`
  - Windows/Linux：`Ctrl+Shift+E`
